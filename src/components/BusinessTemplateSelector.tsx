/**
 * Stub component - Template selector removed since we standardized on healthcare.
 */
export function BusinessTemplateSelector(_props: {
    selectedTemplate?: string;
    onSelectTemplate?: (template: string) => void;
}) {
    // No-op: template selection is no longer used
    return null;
}
