/**
 * Stub component - Template preview removed since we standardized on healthcare.
 */
export function TemplatePreview(_props: {
    template?: string;
    className?: string;
}) {
    // No-op: template preview is no longer used
    return null;
}
