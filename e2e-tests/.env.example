/**
 * Test Environment Configuration
 * 
 * Environment-specific settings for running the AI browser testing agent
 */

// Base URLs for different environments
TEST_BASE_URL=http://localhost:5173

# Supabase Test Credentials (replace with actual test account credentials)
TEST_DENTIST_EMAIL=dentist-test@caberu.be
TEST_DENTIST_PASSWORD=TestPassword123!

TEST_PATIENT_EMAIL=patient-test@caberu.be
TEST_PATIENT_PASSWORD=TestPassword123!

TEST_ADMIN_EMAIL=admin-test@caberu.be
TEST_ADMIN_PASSWORD=TestPassword123!

# Test clinic slug for booking tests
TEST_CLINIC_SLUG=test-clinic

# CI Configuration
CI=false
